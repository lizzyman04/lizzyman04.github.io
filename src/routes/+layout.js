// Static SPA (GitHub Pages): no server to render on, so disable SSR and
// prerendering. The Laravel API is fetched in the browser at runtime.
export const prerender = false;
export const ssr = false;
